name: 🤖 AI Apply Safe Fixes

on:
  pull_request:
    types: [labeled]

permissions:
  contents: write
  pull-requests: write

jobs:
  apply:
    if: contains(github.event.pull_request.labels.*.name, 'apply-ai-fixes')
    runs-on: ubuntu-latest
    env:
      ENABLE_E2E: true
      PLAYWRIGHT_BASE_URL: http://localhost:3000
      PLAYWRIGHT_WEB_SERVER: node scripts/simple-static-server.js
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install deps
        run: npm ci --no-audit

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run E2E (Chromium) to generate artifacts (non-blocking)
        run: |
          npm run test:e2e || true

      - name: Run auto-heal demo to export learnings (non-blocking)
        run: |
          npm run ai:auto-heal || true

      - name: Apply safe fixes (lint/format)
        run: |
          npx eslint . --fix || true
          npx prettier --write "**/*.{js,jsx,ts,tsx,json,md,css,scss}" || true

      - name: Apply auto-heal learnings (if present)
        run: |
          if [ -d test-results ]; then
            npm run ai:apply-heal || true
          else
            echo "No test-results/ learnings to apply."
          fi

      - name: Create PR with changes
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "chore(ai): apply safe auto-fixes"
          title: "chore(ai): apply safe auto-fixes"
          body: "Automated lint/format and optional selector healings."
          branch: ai/apply-safe-fixes
          base: ${{ github.event.pull_request.head.ref }}
          labels: ai, automation
